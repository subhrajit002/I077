leardership.js
import LeadershipSection from "../components/LeadershipSection";
import PhotoCarousel from "../components/PhotoCarousel";
import "../styles/global.css"

export default function LeadershipPage() {
    return (
        <div className="container-fluid p-0">
            <header className="bg-dark text-white text-center py-5">
                <h1 className="display-4 fw-bold">Our Leadership Team</h1>
                <p className="lead">Meet the visionaries behind our success</p>
            </header>

            <main className="container py-5">
                <LeadershipSection />

                <section className="mt-5 pt-5 border-top">
                    <h2 className="text-center mb-4 fw-bold">Company Highlights</h2>
                    <p className="text-center mb-5 text-muted">Glimpses of our journey, culture, and achievements</p>
                    <PhotoCarousel />
                </section>
            </main>

            <footer className="bg-dark text-white text-center py-4 mt-5">
                <p className="mb-0">© 2024 Company Name. All rights reserved.</p>
            </footer>
        </div>
    )
}


leardershipSection.js
import React from "react"
import LeadershipCard from "./LeadershipCard"
import image from "../components/subhrajit.jpeg";

export default function LeadershipSection() {
    const leadershipTeam = [
        {
            id: 1,
            name: "Sarah Johnson",
            position: "Chief Executive Officer",
            photo: image,
            description: "With over 20 years of industry experience, Sarah leads our company with vision and strategic insight.",
        },
    ]

    return (
        <section className="leadership-section">
            <div className="row g-4">
                {leadershipTeam.map((leader) => (
                    <div key={leader.id} className="col-md-6 col-lg-4">
                        {/* <LeadershipCard leader={leader} /> */}
                        <LeadershipCard leader={leader} />
                    </div>
                ))}
            </div>
        </section>
    )
}


learderCard.js
import React from "react"

export default function LeadershipCard({ leader }) {
    return (
        <div className="card h-100 shadow-sm border-0 leadership-card">
            <div className="text-center p-3">
                <div className="position-relative mx-auto" style={{ width: "180px", height: "180px" }}>
                    <img
                        src={leader.photo }
                        alt={leader.name}
                        className="rounded-circle border border-3 border-light shadow"
                        style={{ width: "100%", height: "100%", objectFit: "cover" }}
                    />
                </div>
            </div>
            <div className="card-body text-center">
                <h3 className="card-title fw-bold">{leader.name}</h3>
                <p className="text-primary fw-semibold mb-3">{leader.position}</p>
                <p className="card-text text-muted">{leader.description}</p>
            </div>
            <div className="card-footer bg-white border-0 text-center pb-4">
                <div className="d-flex justify-content-center gap-3">
                    {/* <a href="#" className="text-dark fs-5"><i className="bi bi-linkedin"></i></a>
                    <a href="#" className="text-dark fs-5"><i className="bi bi-twitter-x"></i></a>
                    <a href="#" className="text-dark fs-5"><i className="bi bi-envelope"></i></a> */}
                </div>
            </div>
        </div>
    )
}


photoCaroseul.js
import React, { useState } from "react"

export default function PhotoCarousel() {
  const companyPhotos = [
    { id: 1, src: "/placeholder.svg", alt: "Company headquarters", caption: "Our modern headquarters in downtown" },
    { id: 2, src: "/placeholder.svg", alt: "Team building event", caption: "Annual team building retreat" },
    { id: 3, src: "/placeholder.svg", alt: "Product launch", caption: "Our latest product launch event" },
    { id: 4, src: "/placeholder.svg", alt: "Community service", caption: "Giving back to our community" },
    { id: 5, src: "/placeholder.svg", alt: "Innovation lab", caption: "Where our best ideas come to life" },
  ]

  const [activeIndex, setActiveIndex] = useState(0)

  const handlePrev = () => {
    setActiveIndex((prev) => (prev === 0 ? companyPhotos.length - 1 : prev - 1))
  }

  const handleNext = () => {
    setActiveIndex((prev) => (prev === companyPhotos.length - 1 ? 0 : prev + 1))
  }

  return (
    <div className="photo-carousel position-relative">
      <div className="carousel slide shadow rounded overflow-hidden">
        <div className="carousel-inner">
          {companyPhotos.map((photo, index) => (
            <div key={photo.id} className={`carousel-item ${index === activeIndex ? "active" : ""}`}>
              <img
                src={photo.src}
                alt={photo.alt}
                className="d-block w-100"
                style={{ height: "500px", objectFit: "cover" }}
              />
              <div className="carousel-caption d-none d-md-block bg-dark bg-opacity-50 rounded py-2">
                <h5>{photo.alt}</h5>
                <p>{photo.caption}</p>
              </div>
            </div>
          ))}
        </div>
        <button className="carousel-control-prev" type="button" onClick={handlePrev}>
          <span className="carousel-control-prev-icon"></span>
        </button>
        <button className="carousel-control-next" type="button" onClick={handleNext}>
          <span className="carousel-control-next-icon"></span>
        </button>
      </div>

      <div className="carousel-indicators mt-3 d-flex justify-content-center">
        {companyPhotos.map((_, index) => (
          <button
            key={index}
            type="button"
            className={`bg-primary ${index === activeIndex ? "active" : ""}`}
            onClick={() => setActiveIndex(index)}
          ></button>
        ))}
      </div>
    </div>
  )
}


global.css
:root {
  --primary-color: #0d6efd;
  --secondary-color: #6c757d;
  --dark-color: #212529;
  --light-color: #f8f9fa;
}

body {
  background-color: #f8f9fa;
}

.leadership-card {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.leadership-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1) !important;
}

.carousel-item img {
  height: 500px;
  object-fit: cover;
}

.carousel-indicators button {
  width: 12px !important;
  height: 12px !important;
  border-radius: 50%;
  background-color: var(--primary-color);
  opacity: 0.5;
  transition: opacity 0.3s ease;
  margin: 0 5px;
}

.carousel-indicators button.active {
  opacity: 1;
}

.photo-carousel {
  margin-bottom: 60px;
}

@media (max-width: 768px) {
  .carousel-item img {
    height: 300px;
  }
}
